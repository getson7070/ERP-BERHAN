services:
  db:
    container_name: db
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_DB: erp_berhan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - erp-berhan_postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  erp-berhan:
    container_name: erp-berhan
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - .:/app
    ports:
      - "18000:8000"
    depends_on:
      - db
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python - <<'PY'\nimport urllib.request,sys\ntry:\n resp=urllib.request.urlopen('http://localhost:8000/readyz',timeout=5)\n sys.exit(0 if resp.status==200 else 1)\nexcept Exception:\n sys.exit(1)\nPY"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  erp-berhan_postgres_data:
