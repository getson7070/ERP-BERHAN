# Core app & server
Flask==3.0.3
gunicorn==22.0.0

# Realtime (progressive: prefers eventlet, falls back to threading)
Flask-SocketIO==5.3.6
python-socketio==5.11.3
python-engineio==4.9.1
eventlet==0.36.1

# Database & migrations (kept even if you don't run them today)
SQLAlchemy==2.0.32
Flask-Migrate==4.0.7
alembic==1.13.2
psycopg2-binary==2.9.9

# Optional scale/observability (harmless if not configured)
redis==5.0.8
sentry-sdk==2.14.0
