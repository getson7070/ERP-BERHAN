version: 1.0
runtime: python3.11
build:
  commands:
    - python -m pip install --upgrade pip
    - python -m pip install --no-cache-dir -r requirements.txt
run:
  command: bash -c "flask --app app db upgrade && gunicorn --bind 0.0.0.0:8000 wsgi:app"
  network:
    port: 8000
