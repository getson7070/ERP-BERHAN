version: 1.0
runtime: python3
build:
  commands:
    - pip install --no-cache-dir -r requirements.txt
run:
  command: flask db upgrade && gunicorn --bind 0.0.0.0:8000 wsgi:app
  network:
    port: 8000
