name: audit-chain
on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install -r requirements.txt
      - run: python scripts/check_audit_chain.py > audit_report.txt
      - uses: actions/upload-artifact@v4
        with:
          name: audit-chain-report
          path: audit_report.txt
