services:
  web:
    build: .
    command: sh -c "gunicorn --config gunicorn.conf.py erp:create_app()"
    ports:
      - "8000:8000"
    environment:
      ENV: production
      DATABASE_URL: ${DATABASE_URL}
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      SECURITY_PASSWORD_SALT: ${SECURITY_PASSWORD_SALT}
      SENTRY_DSN: ${SENTRY_DSN}
      BACKUP_ENCRYPTION_KEY: ${BACKUP_ENCRYPTION_KEY}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    security_opt:
      - no-new-privileges:true
      - seccomp:./deploy/seccomp.json
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
  worker:
    build: .
    command: sh -c "celery -A erp.celery_app.celery_app worker --loglevel=info"
    environment:
      ENV: production
      DATABASE_URL: ${DATABASE_URL}
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      SENTRY_DSN: ${SENTRY_DSN}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    security_opt:
      - no-new-privileges:true
      - seccomp:./deploy/seccomp.json
    healthcheck:
      test: ["CMD", "celery", "-A", "erp.app.celery", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
  beat:
    build: .
    command: sh -c "celery -A erp.celery_app.celery_app beat --loglevel=info"
    environment:
      ENV: production
      DATABASE_URL: ${DATABASE_URL}
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/0
      SENTRY_DSN: ${SENTRY_DSN}
    depends_on:
      worker:
        condition: service_healthy
    restart: always
    security_opt:
      - no-new-privileges:true
      - seccomp:./deploy/seccomp.json
    healthcheck:
      test: ["CMD", "celery", "-A", "erp.app.celery", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
  backup:
    build: .
    command: sh -c "while true; do python backup.py; sleep 86400; done"
    environment:
      ENV: production
      DATABASE_URL: ${DATABASE_URL}
      BACKUP_ENCRYPTION_KEY: ${BACKUP_ENCRYPTION_KEY}
      S3_BUCKET: ${S3_BUCKET}
      S3_ENDPOINT: ${S3_ENDPOINT}
      AWS_REGION: ${AWS_REGION}
      SENTRY_DSN: ${SENTRY_DSN}
    depends_on:
      db:
        condition: service_healthy
    restart: always
    security_opt:
      - no-new-privileges:true
      - seccomp:./deploy/seccomp.json
  monitor:
    build: .
    command: sh -c "while true; do python scripts/monitor_queue.py; sleep 60; done"
    environment:
      ENV: production
      CELERY_BROKER_URL: redis://redis:6379/0
      REDIS_URL: redis://redis:6379/0
      ALERT_EMAIL: ${ALERT_EMAIL}
      SLACK_WEBHOOK: ${SLACK_WEBHOOK}
    depends_on:
      web:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always
    security_opt:
      - no-new-privileges:true
      - seccomp:./deploy/seccomp.json
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./deploy/init-app-user.sh:/docker-entrypoint-initdb.d/init-app-user.sh:ro
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d $${POSTGRES_DB:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
  redis:
    image: redis:7
    restart: always
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
volumes:
  pgdata:
  redisdata:



