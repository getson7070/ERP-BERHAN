{
  "required": [
    "APP_ENV",
    "FLASK_ENV",
    "FLASK_DEBUG",
    "SECRET_KEY",
    "DATABASE_URL",
    "REDIS_URL",
    "JWT_SECRET",
    "CSRF_SECRET",
    "SECURE_PROXY_SSL_HEADER",
    "RATELIMIT_DEFAULT",
    "RATELIMIT_STORAGE_URI",
    "DEPLOY_HOOK_URL",
    "TELEGRAM_BOT_TOKEN",
    "NETLIFY_SITE_ID",
    "NETLIFY_AUTH_TOKEN"
  ],
  "notes": "Defines required env keys that must appear in example env files. No secrets are committed."
}